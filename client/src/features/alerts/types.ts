/** Options for alert notification history hooks. */
export type UseAlertNotificationsOptions = {
  enabled?: boolean
}

/** Options for price alert CRUD hooks. */
export type UsePriceAlertsOptions = {
  enabled?: boolean
}
